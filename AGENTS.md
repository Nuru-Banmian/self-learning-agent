# 项目协作指南

探索项目或规划实现前，先阅读 `HANDOFF.md`，了解已确认的需求、约束和待定事项。

## Agent skills

### Issue tracker

需求、规格和开发任务使用 GitHub Issues；操作前阅读 `docs/agents/issue-tracker.md`。

### Triage labels

分诊使用五个默认标签；应用或修改标签前阅读 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 布局：根目录 `CONTEXT.md` 和 `docs/adr/`；探索代码或讨论设计前阅读 `docs/agents/domain.md`。
