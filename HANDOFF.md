# 自我学习多 Agent 个人助理：开发交接文档

记录日期：2026-09-20
来源：本次产品讨论。本文记录需求与决定，不代表功能已经实现。
当前阶段：已实现 Issue #4 的普通聊天记忆提取、范围期限校验、跨会话普通问答迁移及记忆面板；也已实现 Issue #2、#3 的聊天待办创建/查询/修改/完成、基础当天计划和建议授权加入、SQLite 持久化、公开 HTTP/SSE 与工作台，并完成本地测试、真实百炼和浏览器验证。首版其他切片仍待实现；大陆出口位置尚未核验。后续各节保留原始规划历史，当前实现状态见 README.md、docs/acceptance/issue-2.md 和 docs/acceptance/issue-3.md 和 docs/acceptance/issue-4.md。

## 0. 后续语音澄清与选型更新（2026-09-20）

本节是后续用户澄清，优先于下文早期记录中的冲突描述。下文第 1 至 9 节保留初始讨论历史；当前开发范围、技术基线、默认行为和验收以 `SPEC.md` 为准。

- 项目来自面试挑战：一天内手搓一个自我学习的生活多 Agent 平台，功能由用户按自用需要定义，用于考察设计、策划与动手能力。
- 产品主线是日常生活规划：聊天记录今天或明天的待办；下次打开后，结合待办与用户记忆生成当天计划，并按事项需要补充资料或天气等信息。
- 学习 Python 时，结合此前说过的具体学习主题查资料并细化计划；出门办事时，结合地点、时间和天气提示需要携带的物品。是否需要后台主动通知仍未确定。
- 三个角色明确为：**主 Agent、执行 Agent、学习 Agent**。早期“研究 Agent”的职责应纳入执行 Agent，执行能力不只限于研究资料。
- **学习 Agent 的核心职责是从平时对话中提取、整理、更新与用户有关的记忆，并在后续任务中使用。** 不以用户必须说“记住”或明确纠正为学习入口；推测性信息如何确认、生效与失效仍需细化。
- 可复用流程是早期讨论过的方向，但是否属于首版必做项待确认；不能把流程教学替代上述日常记忆能力。
- 用户确定使用 **阿里云百炼 API**，并授权开发助理选择具体模型。已选择 **Qwen3.7-Plus 固定快照 `qwen3.7-plus-2026-05-26`**，首版三个角色共用，默认关闭深度思考；接入地域、密钥与账户可用性尚未验证。
- 用户随后表示技术栈已基本确定，并要求 **所有外部 API 在中国大陆网络可正常使用**。据此将无需代理的大陆网络可用性列为硬性验收条件；当前没有完成账户真实调用验证。
- 技术基线为 Python/FastAPI、React/TypeScript/Vite、SQLite、自写轻量调度和 SSE；具体版本与工程配置待实现前锁定。
- 根据大陆使用约束，搜索选用 **阿里云 IQS UnifiedSearch**，天气选用 **和风天气**；此前提出的 Tavily、Open-Meteo 不再作为首版默认依赖。百炼优先采用北京地域接入，模型固定快照保持不变。
- IQS 需要单独开通并创建其 API Key；和风需要独立帐号、项目凭据和专属 API Host。服务开通、权限、配额与真实费用尚未验证。选择依据与限制见 `SPEC.md` 的 Implementation Decisions 与 Further Notes。
- 用户随后接受将真实联调提前：**先搭通第一条核心流程，随即接入 API，边开发边验证。** 用户愿意为加快开发提前把 API 凭据提供到本地配置中；当前尚未实际提供。模拟测试与本地验证仍可先行，但不能代替真实接入验收。当前正在评估进入开发的条件，尚未开始应用实现。
- 用户已明确要求开发助理创建 GitHub 仓库，并通过调用 to-spec 授权将规格发布到项目 Issue tracker。本次创建和发布不包含应用实现。
- 私有仓库：[Nuru-Banmian/self-learning-agent](https://github.com/Nuru-Banmian/self-learning-agent)，本地已初始化并关联远程；初始目录中仅有文档，没有既有应用代码需要兼容。
- 正式规格：[实现自我学习生活多 Agent 个人助理首版](https://github.com/Nuru-Banmian/self-learning-agent/issues/1)，已标记 ready-for-agent；正文与 `SPEC.md` 同步。

## 1. 已确认的目标与约束

- 做一个主要供用户自己使用的个人助理工作台，同时用于简历和面试展示。
- 功能由用户根据自己的需要定义，平台支持后续逐步增加能力。
- 核心要求是 **Agent 能从与用户的对话、纠正和反馈中学习，并改变后续处理任务的方式**。
- “自我学习”指 Agent 的学习能力；项目并非仅指帮助用户管理学习计划的平台。
- 采用多 Agent 分工，同时保留一个统一的聊天入口。
- 用户希望在一天内做出可演示的原型。这是首版范围约束，不是完整平台的交付承诺；具体起止时间尚未确定。
- 用户计划使用 Codex 辅助编写代码，并在仓库初始化后使用 **Skio** 辅助开发。
- 用户表示会先自行初始化仓库。本次交接工作仅创建文档，不初始化仓库或开始实现。

## 2. 已选择与已排除的方向

用户明确选择了此前讨论中的想法一、二、六，并新增 To-Do 作为核心使用场景。

| 方向 | 在本项目中的定位 |
| --- | --- |
| 想法一：通过聊天教 Agent 新技能 | 将用户教给它的处理方法保存成可复用流程，后续可调用、纠正和更新 |
| 想法二：会积累研究经验的研究助理 | 根据当前任务调用工具收集资料，给出有来源的结果，并应用学到的研究方法 |
| 想法六：能观察学习效果的实验台 | 展示反馈、提炼出的经验及后续应用情况，通过新任务检查学习有没有作用 |
| 新增：对话式 To-Do | 记录未来要做的事；在新对话中查询今日任务，再结合其他工具生成建议 |

用户明确表示想法三、四、五没有必要：写作工作室、开发排错助手、信息筛选助手不作为当前独立功能方向。

## 3. 核心使用流程

### 3.1 通过对话记录待办

示例输入：

> 明天我要学习 Agent 的记忆机制，还要把项目首页搭出来。

预期行为：

1. 识别并保存两条任务。
2. 将“明天”转换为具体日期，并向用户反馈实际保存的内容。
3. 任务持久保存，重新打开应用或开启新对话后仍可查询。
4. 用户后续可以通过对话查询、修改或标记完成。

示例仅用于说明流程，不是需要预先写入应用的真实任务。

### 3.2 查询今天应该做什么

用户开启新对话，询问：

> 今天我该干什么？

预期流程：

1. 查询今日未完成任务，单独列出逾期未完成事项。
2. 读取与本次任务相关的偏好、经验和技能。
3. 根据任务需要调用其他工具收集信息；首版优先接入网页搜索。
4. 结合真实任务、工具结果和已学经验，给出行动建议。

回答需要区分：已经存在的待办、外部查询到的信息、Agent 提出的建议。资料附来源；工具结果不可凭空编造。

例如，发现“学习记忆机制”后，可以查找相关文档和例子，再建议从一个具体练习开始。工具按任务需要调用，并非每次查询 To-Do 都必须搜索。

### 3.3 将建议转成任务

Agent 的建议先作为建议展示。用户说“加进去”等明确指令后，再新增为待办；修改原任务同样依据用户的修改指令。

查询“今天做什么”主要完成查询、信息收集与建议生成，不代表用户已经要求执行完所有待办。

## 4. 自我学习的首版定义

首版采用 **长期记忆与行为规则更新**：从对话反馈提炼内容，持久保存，在后续相关任务中读取并应用。底层模型参数保持不变。

面试官对“自我学习”的正式验收定义尚未提供。当前方案不等同于模型微调、强化学习训练或自动修改自身代码；若面试要求涉及这些能力，需要另行调整范围。

### 4.1 学习偏好

例子：

> 以后推荐技术资料，优先官方文档。

后续处理不同技术主题时，应优先采用这一偏好。

### 4.2 学习处理任务的方法

例子：

> 以后帮我规划今天时，先查未完成任务，再按需要查资料，最后给三个行动建议。

将这套方法保存成可复用流程，换一组任务、换一个会话后仍可应用。首版技能可以基于已有工具组合处理步骤，具体表示和执行方式尚未选定。

### 4.3 明确区分不同内容

| 内容 | 例子 | 预期用途 |
| --- | --- | --- |
| 任务事实 | 明天完成项目首页 | 进入待办，记录日期与完成状态 |
| 长期偏好 | 以后优先推荐官方资料 | 在相关任务中持续采用 |
| 临时条件 | 今天只有半小时 | 限于当前日期或本次任务 |
| 可复用方法／技能 | 排计划前先查旧任务 | 影响处理步骤和工具使用 |
| 研究结果与建议 | 查到的文档、建议新增的练习 | 为当前建议提供依据，区别于用户已安排的任务 |

只从明确反馈开始学习。用户应能查看、修改和删除保存的规则；纠正已有偏好时更新规则，避免同时采用互相矛盾的旧规则。

### 4.4 学习效果如何展示

至少能回答：

- 从用户哪句反馈学到了什么？
- 本次任务采用了哪条经验或哪个流程？
- 换一个新对话、同类的新任务后，行为是否发生了预期变化？

学习验证依据任务结果、实际工具调用和明确检查点。Agent 自己声称“我学会了”不能作为验收证据。

## 5. 已接受的首版角色与界面建议

用户在以下方案之后确认“OK”，因此将其作为当前开发基线；具体实现技术仍待选择。

| 角色 | 职责 |
| --- | --- |
| 主助理 | 与用户对话，查询和修改待办，选择所需工具或委派研究任务，组织最终建议 |
| 研究 Agent | 接收明确的信息收集任务，查询资料，返回带来源的结果 |
| 学习 Agent | 分析用户纠正，提炼或更新偏好、经验和可复用流程 |

学习效果验证可直接依据记录和检查逻辑实现，不要求额外增加一个评测 Agent。多 Agent 不意味着必须使用不同模型；调用组织方式尚未确定。

首版界面包括：

- 聊天区：统一输入任务、查询、反馈和教学指令。
- 待办列表：展示已保存的事项、日期和完成情况。
- “学到了什么”面板：展示规则、来源反馈及应用情况，支持规则修改与删除。
- 简单的学习前后对比展示：服务面试演示，不要求第一天完成独立的大型实验平台。

## 6. 一天原型的优先范围

1. 对话创建、查询、修改、完成待办，并实现跨会话与应用重启后的持久保存。
2. 查询今日任务，区分逾期未完成事项。
3. 接入一种真实的信息工具，优先网页搜索，为相关任务补充带来源的资料。
4. 从明确反馈中保存偏好或处理规则，在新对话中应用；支持查看、修改和删除。
5. 用一个可复用流程贯穿演示，例如“查任务 → 按需查资料 → 给行动建议”。
6. 用新的同类任务展示学习效果，并完成上述三个主要界面区域。

选定的是可扩展的个人工作台方向；第一天先完成这条端到端流程，再扩大技能与工具数量。

## 7. 建议的验收检查

以下是将已讨论行为转成可检查条件的开发建议，尚无实现或测试结果。

| 场景 | 通过条件 |
| --- | --- |
| 跨会话记任务 | 创建未来日期的任务后，重启应用、开启新对话，仍能查到准确内容与日期 |
| 今日查询 | 在受控日期下，今日未完成、逾期未完成及已完成任务被正确区分 |
| 任务修改 | 修改日期或标记完成后，再查询反映真实的新状态 |
| 信息收集 | 任务需要资料时发生真实工具调用，建议能追溯来源；失败时明确说明缺少的信息 |
| 建议边界 | 生成建议本身不新增或改写待办；用户要求加入或修改后才持久更新 |
| 学习迁移 | 用户提供纠正后，在不携带完整旧聊天记录的新会话中，对新的同类任务正确应用保存的规则 |
| 规则维护 | 新反馈可更新旧规则；删除后后续调用不再加载该规则；一次性要求不会被当作永久偏好 |
| 学习可见性 | 能对应展示原始反馈、保存的规则、实际采用情况和结果中的变化 |

演示与交接时应区分真实调用、模拟结果及未验证部分。验收重点是可观察行为，不依赖模型回答的固定措辞。

## 8. 尚未决定的事项

- 正式项目名、仓库名、远程托管位置。
- 编程语言、前后端框架、Agent 框架及编排方式。
- 模型服务商、模型型号、API 接入方式和可用凭据。
- 持久化方案、数据结构、记忆检索方式、技能表示与执行方式。
- 网页搜索工具的具体供应商及网页读取能力。
- 自然语言日期的解释规则、用户时区配置、未指定日期时的交互方式。
- 首个演示技能的最终内容及学习效果的具体检查点。
- 部署形式、登录或多用户需求；当前用途以用户自用为前提。
- 面试官对“自我学习”的确切要求。
- **Skio 的具体工具身份、拼写与使用流程。** 本文保留用户原称，不将其自动替换成其他工具，也不假定已经安装或配置。

这些事项不能从当前对话推导为已定技术选型。后续根据用户初始化后的仓库状态、工具配置及补充要求确定。

## 9. 当前状态与接手顺序

文档创建前，当前工作目录没有项目文件。本次仅整理交接文档，没有编写应用、运行应用测试、初始化 Git 仓库或配置 Skio。

建议接手顺序：

1. 用户完成仓库初始化；开发助理读取本文件及届时已有的仓库说明、配置。
2. 结合现有配置确定必要技术选择，核实 Skio 的具体使用方式。
3. 先跑通“记任务 → 持久保存 → 新会话查任务”，以能读回真实任务为完成条件。
4. 接入信息工具和建议生成，以真实查询结果及来源能出现在回答中为完成条件。
5. 接入学习与流程复用，以新会话中的新任务确实采用反馈形成的规则为完成条件。
6. 完成界面与验收演示，记录实际通过项、失败项和未完成项。

后续修改以用户的新决定为准，并同步更新本文中受影响的范围与待定项。
