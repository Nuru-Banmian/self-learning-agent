# 自我学习生活多 Agent 个人助理：首版规格

状态：首版开发规格；Issue #2、#3 的待办与基础计划、Issue #4 的普通聊天记忆与跨会话使用已实现，其余首版能力待实现。实测范围见 docs/acceptance/issue-2.md、issue-3.md 和 issue-4.md。
整理日期：2026-09-20。
本文综合初始交接、后续语音澄清与技术选型，作为首版开发和验收的统一依据。后续澄清优先于早期讨论；本文不包含已经完成开发或真实测试的声明。

## Problem Statement

用户在日常聊天中提到今天、明天或以后想做的事情，但这些安排容易留在聊天记录里，重新打开应用后仍需要自己整理。只有待办清单也不够：学习某个 Python 主题需要找到资料和练习，准备外出需要结合目的地、日期和天气决定如何准备。

用户希望个人助理记录这些安排，在下一次使用时结合真实待办生成当天计划，并逐渐记住自己的背景、偏好和时间条件，减少反复解释。“自我学习”是从日常对话中积累、整理和应用用户记忆，底层模型参数保持不变。

项目来自一天内手搓自我学习生活多 Agent 平台的面试挑战，功能由用户按自用需要定义，展示设计、策划和动手能力。首版必须提供可验证的真实闭环；未提供的正式面试评分标准不能被视为已满足。

## Solution

构建主要供用户自己使用的网页工作台，包含统一聊天区、待办列表和“学到了什么”记忆面板。用户通过聊天记录安排；在新会话中询问今天应该做什么时，系统读取待办和相关记忆，按需获取资料或天气，给出有依据的当天计划。

三个角色协作完成：

- **主 Agent**：与用户对话，处理待办意图，结合记忆组织计划，按需委派执行并汇总实际结果。
- **执行 Agent**：根据明确任务调用网页搜索、地点查询和天气等工具，返回带来源的实际结果。
- **学习 Agent**：从普通日常对话中提取、整理和更新用户记忆，供之后的对话与任务使用。用户不必说“记住”才能形成记忆。

首版主线：聊天记录待办和用户信息 → 持久保存 → 重新打开或开启新会话 → 读取真实待办与相关记忆 → 按需搜索或查天气 → 生成当天计划 → 用户纠正信息或明确要求修改待办 → 后续行为反映新状态。

验收覆盖两个代表场景：

1. **学习安排**：记住要学的 Python 主题和资料偏好，找到有来源的资料，细化步骤并提出可选练习。
2. **外出准备**：根据出行事项、目的地和日期获取天气，提出携带物品等准备提示。提示属于当前计划内容，首版不做后台推送。

所有外部 API 都必须在实际使用的中国大陆网络直接可用，不依赖代理或 VPN。选择国内供应商是选型依据，真实可用性仍须实际调用验证。

## User Stories

### 日常对话与待办

1. 作为个人使用者，我希望通过一个聊天入口表达安排和提出问题，以便不必切换不同 Agent 对话。
2. 作为个人使用者，我希望说“明天学习 Python 生成器”后得到一条真实保存的待办，以便以后能继续查询。
3. 作为个人使用者，我希望一句话中的多项安排分别成为待办，以便各自修改和完成。
4. 作为个人使用者，我希望“今天”“明天”被转换成具体日期并反馈，以便及时发现日期误解。
5. 作为个人使用者，我希望没有日期的事项保留为未安排，以便不会被擅自安排到今天。
6. 作为个人使用者，我希望日期或修改对象不明确时得到简短追问，以便避免改错事项。
7. 作为个人使用者，我希望开启新会话后仍能查询过去的待办，以便安排不依赖旧聊天上下文。
8. 作为个人使用者，我希望应用重启后待办仍然存在，以便正常关闭应用不会丢失安排。
9. 作为个人使用者，我希望区分今日未完成、逾期未完成和未安排事项，以便分清状态。
10. 作为个人使用者，我希望已完成事项不混入今日未完成清单，以便计划反映实际进度。
11. 作为个人使用者，我希望通过聊天修改待办标题或日期，以便调整已有安排。
12. 作为个人使用者，我希望标记待办完成后能读回状态，以便确认修改已保存。
13. 作为个人使用者，我希望网络重试不会重复新增待办，以便一次请求只产生一次预期写入。

### 当天计划与执行

14. 作为个人使用者，我希望询问“今天应该做什么”时得到基于真实待办的计划，以便直接着手行动。
15. 作为个人使用者，我希望计划使用相关背景、偏好和临时条件，以便建议适合实际情况。
16. 作为个人使用者，我希望学习主题得到资料和具体步骤补充，以便把笼统待办变成行动。
17. 作为个人使用者，我希望系统按任务需要搜索，以便减少无关结果和等待。
18. 作为个人使用者，我希望研究资料附来源链接，以便核查建议的依据。
19. 作为个人使用者，我希望系统区分搜索摘要和已读取的网页正文，以便了解依据的充分程度。
20. 作为个人使用者，我希望外出安排结合目的地和日期查询天气，以便提前准备。
21. 作为个人使用者，我希望重名地点得到澄清，以便不会使用另一个城市的天气。
22. 作为个人使用者，我希望无法取得对应日期天气时得到明确说明，以便不依赖虚构预报。
23. 作为个人使用者，我希望已有待办、外部信息和新建议清楚区分，以便知道哪些是已安排事项。
24. 作为个人使用者，我希望生成计划不自动新增或修改待办，以便保留对安排的控制。
25. 作为个人使用者，我希望明确要求加入某个练习后才新增待办，以便把认可的建议转为安排。

### 日常对话形成与维护记忆

26. 作为个人使用者，我希望普通聊天中的相关背景和偏好被整理，以便不必每次使用教学口令。
27. 作为个人使用者，我希望用户记忆与待办分别保存，以便偏好不会被当成待完成任务。
28. 作为个人使用者，我希望“今天只有半小时”只影响相应日期，以便临时条件不会永久限制计划。
29. 作为个人使用者，我希望模型猜测和网页内容不被当成我的事实，以便记忆可信。
30. 作为个人使用者，我希望每条记忆能追溯来源对话，以便理解系统为何这样认识我。
31. 作为个人使用者，我希望查看和编辑保存的记忆，以便纠正理解偏差。
32. 作为个人使用者，我希望新的明确纠正更新对应旧信息，以便不同时采用矛盾内容。
33. 作为个人使用者，我希望删除记忆后后续任务不再加载，以便控制系统使用的信息。
34. 作为个人使用者，我希望删除的记忆不会因重处理旧消息自动恢复，以便删除具有持续效果。
35. 作为个人使用者，我希望当前任务的明确要求优先于一般偏好，以便表达这次的例外。
36. 作为个人使用者，我希望重启和新会话中仍能使用已保存记忆，以便持续获得个性化帮助。
37. 作为个人使用者，我希望记忆保存失败时被明确告知，以便不会误以为系统已记住。

### 可见性、可靠性与交付

38. 作为个人使用者，我希望看到当前处理角色、实际工具和完成状态，以便理解等待和结果。
39. 作为个人使用者，我希望看到本次加载和采用的记忆，以便判断系统是否使用了过去的信息。
40. 作为面试演示者，我希望用新会话中的新任务展示学习迁移，以便证明效果不依赖重放完整旧聊天。
41. 作为面试演示者，我希望固定模型版本并保存学习前后证据，以便区分记忆变化和模型变化。
42. 作为个人使用者，我希望工具失败时仍能查看已保存待办和记忆，以便基础数据查看不被阻断。
43. 作为个人使用者，我希望系统保留有效结果并说明缺失信息，以便部分失败不会包装成全部成功。
44. 作为个人使用者，我希望模型和工具调用有次数与超时限制，以便任务不会无限等待和消耗费用。
45. 作为中国大陆使用者，我希望各类外部接口在无代理环境实际可用，以便正常使用。
46. 作为项目配置者，我希望在本地配置服务凭据和地址，以便使用自己的账户且凭据不进入浏览器或公开仓库。
47. 作为项目开发者，我希望核心流程跑通后立即真实联调，以便尽早发现模型和接口问题。
48. 作为项目验收者，我希望测试报告区分模拟、真实、通过、失败和未验证，以便判断实际交付状态。

## Implementation Decisions

### 已确认约束与规格默认值

已确认的方向、三个角色、技术栈、阿里云模型选型、中国大陆 API 可用性和提前真实联调，直接作为本规格约束。

为使首版可实现，本规格将已讨论的建议收口为以下默认值：本机浏览器单人使用；时区可配置且初始为 Asia/Shanghai；无日期待办进入未安排；模糊修改目标先澄清；自动提取用户记忆但不把模型推测当作事实。这些是本规格采用的工程和产品默认值，不冒充用户曾逐项明确选择的历史决定。需要公开部署或改变默认值时，应显式修订范围。

### 技术基线

| 部分 | 选择与约束 |
| --- | --- |
| 前端 | React、TypeScript、Vite，提供聊天、待办、记忆面板和运行状态 |
| 后端 | Python、FastAPI，一个应用进程内组织三个角色 |
| 编排 | 自写轻量调度，按需委派，不要求每条消息调用全部角色 |
| 持久化 | SQLite 保存会话、消息、待办、用户记忆和必要执行记录 |
| 交互 | HTTP 提交操作和读取数据，SSE 增量返回对话与执行事件 |
| 模型 | 阿里云百炼固定快照 qwen3.7-plus-2026-05-26，三个角色首版共用 |
| 模型模式 | 默认显式关闭深度思考；支持工具调用及学习 Agent 的严格结构化输出 |
| 模型接入 | 首选北京地域兼容接口，密钥与地址必须属于同一地域 |
| 搜索 | 阿里云 IQS UnifiedSearch，作为执行 Agent 的独立工具 |
| 城市与天气 | 和风天气城市搜索和当前天气预报接口，使用账户专属访问域名 |

依赖版本在实现时选择兼容稳定版本并锁定。规格不把未验证的账户权限、限流、时延或准确率当成实测事实。

### 模块和职责

| 模块 | 职责 | 对外承诺 |
| --- | --- | --- |
| 工作台与公开应用接口 | 会话、对话、待办和记忆操作，运行状态展示 | 页面与测试使用相同业务路径，读回真实状态 |
| 对话运行模块 | 装配相关上下文，调用主 Agent，按需委派，汇总终态 | 有限调用、可追踪结果、如实反馈部分失败 |
| 待办管理模块 | 创建、查询、修改、完成，日期和目标校验 | 按用户指令写入，重复请求不重复生效 |
| 用户记忆模块 | 校验并保存学习 Agent 候选变更，检索与维护记忆 | 保留来源和范围，只有生效内容进入后续上下文 |
| 模型与信息工具适配 | 调用实际服务，统一结果和错误 | 不伪造结果，限制超时重试，保留来源 |

主 Agent 提出待办操作和执行委派；执行 Agent 首版使用搜索、地点、天气等读取工具；学习 Agent 提出记忆变更。实际写入由应用校验后完成，角色提示词本身不能扩大权限。三个角色分别组织职责、上下文和工具范围，不依赖三个独立服务。

### 数据模型

当前没有既有应用数据或数据库迁移需要兼容。首版至少保存：

| 记录 | 必要信息 |
| --- | --- |
| 会话与消息 | 稳定标识、角色、内容、接收时间、所属会话 |
| 待办 | 稳定标识、标题、可空安排日期、待完成或已完成状态、来源、创建及更新时间 |
| 用户记忆 | 稳定标识、规范化内容、类别、适用范围、有效时间、生效状态、来源消息、更新时间 |
| 任务执行 | 请求标识、所属会话、角色和工具事件、结果状态、已完成写入、错误摘要、可用的耗时和用量 |
| 记忆使用证据 | 本轮加载的生效记忆、结果采用说明、检查点的通过或未验证状态 |

待办是计划事实的唯一来源；记忆不暗中维护第二份待办真相。会话历史是来源证据，不等同于跨会话记忆。服务凭据仅由后端本地私密配置加载，不进入模型上下文、前端响应、普通运行日志或仓库内容。

### 待办、日期与授权

- 相对日期以消息接收时的用户时区为准；今日边界是该时区零点；保存后反馈具体日期。
- 无日期事项进入未安排，不擅自算作今天；不能可靠解释的日期追问。
- 今日未完成、逾期未完成、未安排和已完成分别查询；计划不自动迁移日期。
- 修改使用稳定任务标识，指代多个任务或建议时先澄清。
- 一次明确的多项创建在单次保存事务中提交；失败不声称全部成功。待办和记忆的不同操作可以分别成功，必须反映各自状态。
- 同一请求标识重试不重复执行已完成写入；新请求中的相同文本不直接当作重复，必要时澄清。
- 生成计划、查资料、提出练习不自动修改待办。用户表达记录、加入、修改或完成的指令后才执行对应写入。

### 用户记忆形成、更新与使用

- 从新增用户消息及必要当前上下文提取背景、偏好和时间条件，不要求教学口令。
- 保存内容必须追溯用户表达；网页、工具响应、助理建议和未经确认的推测不成为用户事实。
- 区分持续偏好、背景、任务范围条件和有期限条件。“今天”在对应本地日期结束后失效，当前任务条件不自动扩大到其他任务。
- 学习 Agent 输出结构化候选变更；应用检查类别、范围、来源、期限与冲突后保存。结构合法不等于内容正确。
- 清晰纠正替换同一范围下的对应旧信息；当前任务的具体要求优先于一般偏好，不因此永久改写一般偏好。无法判断替换或例外时澄清。
- 面板支持查看、编辑和删除。删除后不再加载，旧消息重处理不自动恢复；用户之后重新表达可以形成新的来源记录。
- 首版根据类别、主题、有效时间和相关性筛选，限制注入记忆数量和上下文预算；排序算法由实现选择，不要求向量数据库。
- 新会话只加载相关持久记忆及必要来源引用，不自动重放完整旧聊天。当前会话历史中的失效内容也不能覆盖当前生效记忆。
- 首版在一轮结束前完成必要学习处理或记录明确失败。可以先流式展示回答，但提交成功后才显示记忆已保存；内存中的后台工作不能视为完成。
- 没有新信息的消息可不产生记忆，无新增不等于失败。

### 执行结果与外部服务

信息工具统一区分成功、无结果、部分成功和错误，附任务输入摘要、实际数据、来源和错误信息。主 Agent 可利用已有结果生成注明缺失信息的建议，不能补造未取得的资料。

**搜索：** IQS 独立开通、独立凭据，不能默认复用百炼密钥。结果保留标题、链接和摘要；需要正文时按需读取并标注材料类型。引擎参数和收费增强功能可配置，首版不默认开启增强摘要。搜索服务可达与结果网站可达分别验证。

**天气：** 确定目的地、经纬度、时区和计划日期后查询。重名或缺少地点时追问，不以未经授权的定位猜测位置。使用目的地对应日期，保留天气、来源和查询时间；超出预报范围、无数据或失败时说明。大陆接口可用性不限制目的地只能位于中国。

**回答：** 已有待办、外部事实和行动建议分开表达；引用关联实际返回资料；准备提示与天气结果一致。不要求固定三条建议或固定措辞。

### 公开接口、流式状态与失败处理

- 公开接口支持会话创建和读取、提交对话、读取执行状态，以及待办和记忆查询维护；页面和测试共用这些接口。
- 对话请求携带会话与请求标识；响应关联实际写入、来源和本轮状态。重复请求返回已有状态或结果，不再次执行成功写入。
- SSE 有序事件覆盖角色处理、工具调用与结果、回复片段、保存结果和本轮终态，与实际执行一致。
- 本轮终态区分完成、部分完成和失败；回答成功但记忆保存失败时显示部分完成。
- 首版同一会话按轮次串行，避免覆盖；其他会话只读取已提交的生效记忆。
- 模型/工具次数、超时、总时限、重试均有限且可配置；具体值经首轮真实联调确定并记录，不承诺未测性能。
- 浏览器断线不撤销已提交操作，重连能读取已保存状态；进程中断时未完成工作显示中断或失败，支持用户重试，不要求自动恢复任意长流程。
- 模型或工具不可用时，待办和记忆列表仍可通过普通数据接口查看；无需模型参与的面板编辑仍走相同校验保存规则。

### 页面与学习证据

聊天区展示消息、计划、来源和进度；待办列表展示日期与完成状态；记忆面板展示内容、来源、范围、有效性及维护入口。

分开呈现“已保存”“本轮加载”“结果采用说明”和“检查点是否通过”。模型声称采用只是说明，不能自动变成效果已验证。学习前后对比使用固定模型版本和明确任务检查点，不建设额外评测 Agent 或大型实验平台。

## Testing Decisions

### 测试入口与原则

拟建立一个最高层应用测试入口：**用户实际使用的浏览器应用，以及页面调用的后端 HTTP/SSE 公开接口**。浏览器验证主流程，公开接口验证日期、持久化、失败和重复请求等行为，不为三个 Agent 建三套独立验收入口。

该入口已向用户说明并征求意见，作为本规格采用的测试方案；不把尚未取得的单独答复写成用户明确确认。

当前只有规划文档，没有既有应用接口、测试文件或测试先例；上述入口是拟建立的公共契约。断言外部行为、保存状态和实际工具效果，不匹配完整回答、提示词、内部类名或私有函数调用顺序。

覆盖工作台、对话运行、待办管理、用户记忆和供应商适配，优先通过相同公开接口验证协作。仅对必要的协议转换和难以覆盖的边界补充聚焦测试，不镜像实现。

- 确定性测试可模拟外部模型和工具响应，但使用真实应用、隔离的真实 SQLite 和正常页面请求，不模拟应用自己的保存成功。
- 重启测试停止并重新启动应用进程，继续使用同一份隔离数据；创建新对象不算重启。
- 真实联调沿用相同公开入口和业务路径，不能另建只供演示通过的路径。
- 模型输出检查来源类型、时间安排、行动步骤、授权写入等可观察约束，不检查固定措辞。

### 验收矩阵

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| AC-01 | 待办完整流程 | 对话创建一项或多项，查询、改日期、完成，列表与后续查询一致 |
| AC-02 | 日期与歧义 | 受控时钟区分今日、逾期、未安排、完成；跨零点正确；日期或目标不明时不猜着修改 |
| AC-03 | 跨会话与重启 | 待办和记忆在新会话、真实进程重启后准确可读，数据与真实用户隔离 |
| AC-04 | 建议与写入 | 生成计划前后待办不变；明确要求加入或修改后发生一次对应写入 |
| AC-05 | 普通对话学习 | 无教学口令的日常陈述形成相关记忆并显示来源；网页和推测不成为用户事实 |
| AC-06 | 记忆维护与范围 | 编辑、纠正、删除影响后续任务；旧消息不恢复已删除记忆；临时条件过期；例外不永久替换一般偏好 |
| AC-07 | 学习迁移与证据 | 新会话新任务不重放完整旧聊天，使用持久记忆；来源、保存、加载、采用与检查结果对应 |
| AC-08 | 三角色与状态 | 角色按需协作，真实工具和保存结果与页面/SSE一致，失败不显示已记住 |
| AC-09 | 资料与来源 | 按需真实搜索，来源对应实际结果；摘要与正文不混淆 |
| AC-10 | 外出与天气 | 明确地点日期后查询对应预报；重名消歧、超范围和无结果有明确表现 |
| AC-11 | 失败与重复请求 | 模型、工具、保存失败各自如实反馈，保留有效部分；重试不重复写入 |
| AC-12 | 大陆真实联调 | 实际大陆无代理网络完成模型工具调用、记忆提取、搜索、城市、天气真实调用，保留结果、状态、耗时 |

### 第一条真实演示主线

以下仅为测试样例，不预填为用户真实数据：

1. 普通聊天表达：“我看技术资料喜欢先看官方文档，再做一个小练习。今天我要学 Python 生成器。”
2. 分别保存今天的待办与有来源的资料偏好，展示实际保存状态。
3. 停止并重启应用，开启新会话，询问今天应该做什么。
4. 主 Agent 读取真实待办和记忆，执行 Agent 实际搜索资料并返回可追溯来源。
5. 回答包含对应主题的步骤、官方资料和可选练习，显示应用的记忆；仅建议时待办不变。
6. 明确要求加入练习后新增一次，标记完成后再次查询，状态正确。
7. 更换学习主题验证迁移，再验证纠正、删除偏好后不继续使用旧内容。

天气是紧接着的第二条真实主线，仍为首版交付要求，不能只完成学习场景就宣称全部验收通过。

### 联调顺序与证据

进入实现时提供不含密钥的配置示例和本地私密配置方式。用户愿意提前准备 API；第一条核心流程可运行、配置就绪后立即真实联调，对搜索和天气增量联调，不等全部界面完成。

交付记录分开列本地确定性测试、模拟供应商测试、真实调用、浏览器验证和大陆无代理测试的通过、失败、跳过及未验证。无法确认测试网络位于大陆时，只报告当前网络结果，不能据此宣称大陆验收通过。

当前没有真实凭据、应用运行或测试结果。缺少凭据时可继续不依赖外部服务的实现，但对应真实调用验收保持未验证。

## Out of Scope

- 模型微调、强化学习训练、自动修改自身代码。
- 独立写作工作室、开发排错助手、信息筛选助手。
- 自定义流程语言、任意代码技能生成、自动安装新工具。
- 大型学习实验平台、额外评测 Agent、复杂自动回归集。
- 向量数据库、大规模记忆排序压缩、按角色自动选择不同模型。
- 多用户、账号体系、对外公开托管、手机专用客户端。
- 后台主动通知、定时执行、自动跑完所有待办。
- 重复事项、精确日程冲突求解、复杂撤销和记忆版本回退。
- 任意长流程自动恢复、分布式任务队列、多进程 Agent 服务化。
- 未经指令发消息、订票、购买、修改外部账户等外部写入。

这些是首版边界，不否定后续扩展。持久保存、记忆纠正、真实工具结果和基本失败处理不得以“后续优化”为由省略。

## Further Notes

### 实施顺序

1. 建最小界面和公开接口，打通待办和记忆的保存、跨会话读取，尽早接真实模型。
2. 接搜索和天气，验证来源、日期、地点及失败处理。
3. 完成待办和记忆维护、运行状态、学习证据，执行重启、迁移和授权边界验收。
4. 汇总演示和测试证据，再按实际使用优化界面、检索、成本和速度。

一天是原型范围约束，不是已经开始倒计时或承诺外部服务开通均在一天内完成；外部权限等条件必须如实记录。

### 外部依赖与未验证条件

- 百炼模型权限、北京地域凭据、访问地址、配额和实际账单尚未验证。
- IQS 单独开通和专用凭据、和风项目凭据和专属域名尚未配置，不能假设共用密钥。
- 模型版本、请求模式和实际用量应记录，不以供应商文档替代项目实测。
- Skio 的身份和用法尚未确认，不作为首版技术或运行依赖。
- 本次工作是整理并发布规格及按用户指令创建仓库，不包括编写应用、部署、开通付费服务或完成真实 API 测试。

### 已核实的官方资料

- [Qwen3.7-Plus 模型及固定快照](https://help.aliyun.com/zh/model-studio/qwen3-7-plus)：列出所选快照及工具调用、结构化输出能力。
- [百炼结构化输出](https://help.aliyun.com/zh/model-studio/qwen-structured-output)：该系列支持严格 JSON Schema；仍需应用校验语义。
- [百炼深度思考](https://help.aliyun.com/zh/model-studio/deep-thinking)：该系列默认开启思考，接入需显式配置关闭。
- [百炼兼容接口](https://help.aliyun.com/zh/model-studio/compatibility-of-openai-with-dashscope)：访问地址与密钥按地域匹配。
- [IQS 开通与凭据](https://help.aliyun.com/zh/document_detail/2870227.html)、[IQS HTTP 接入](https://help.aliyun.com/zh/document_detail/3029866.html)：独立服务配置、搜索及网页读取接口。
- [和风开始使用](https://dev.qweather.com/docs/start/)、[专属域名](https://dev.qweather.com/docs/configuration/api-host/)、[城市搜索](https://dev.qweather.com/docs/api/geoapi/city-lookup/)、[每日预报](https://dev.qweather.com/docs/api/weather/weather-daily-forecast/)：账户配置、地点解析与天气。

资料核实日期为 2026-09-20。实现时仍应核实当前接口和账户能力，并完成真实联调。
