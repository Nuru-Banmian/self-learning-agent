# Issue #4：普通聊天记忆与跨会话使用

日期：2026-09-20。审查基线：`4728886b091214de3c691ee2f2a462f24c344892`。

## 已实现路径

公开 messages HTTP → 学习 Agent 严格 JSON 候选 → 应用校验原文、类别、范围、时间、冲突 → SQLite 事务及提交事件 → 限量检索 → 主 Agent 普通回答 → 加载/采用证据 → SSE 终态。读回通过 `/api/memories`、`/api/runs/{id}` 和工作台。

学习只看到当前用户消息及必要待办 ID/标题；没有待办写入或记忆写入工具。主 Agent 有待办工具及普通问答工具，不能提交记忆候选。记忆引用保留消息/会话 ID，最多 6 条、4000 字符，不重放完整旧会话。

## 自动化与真实进程

- `tests/test_memory.py`：30 项 HTTP/SSE 测试通过。覆盖普通偏好、错误来源/类别/范围/期限、引用及假设拒绝、本地零点、任务完成失效、任务条件不扩散、空候选、无效结构、存储失败和幂等重放、潜在冲突、跨日期新条件、上下文数量预算及当前例外。
- `tests/test_process.py`：真实启动、终止、重启 Uvicorn，在新会话问题中读回原记忆 ID、来源和采用说明；旧待办重启用例也通过。供应商在这些自动化测试中是 HTTP 模拟服务。
- 存储故障使用隔离 SQLite 的触发器注入，断言全部经公开接口：无 `memory_saved`、无实际新增、`partial` 终态；同一请求不重新学习。
- 开始时迁移测试失败；新增中文主题匹配回归后通过。跨时区过期条件冲突测试曾失败；改用带时区 datetime 比较后通过。当前例外和有日期的任务范围回归均先失败，修复后通过。

最终全量验证：`pytest -q` **66 passed**（19.89 秒）；mypy、Ruff、format、前端 TypeScript/Vite 构建和 `git diff --check` 均通过。2 条现有依赖弃用警告来自 Starlette/httpx 与 AnyIO，不是测试失败。

## Standards

双轴审查的规范轴：0 项发现；修复后的 `memory_policy.py` 提取也已复核，未发现可执行的规范违反或明显 smell。

## Spec

规格轴初次发现 3 项 P2：跨句限定丢失、未登记任务条件扩散、居住地属性冲突漏检。补回归后修复；复核又发现相邻限定的连词和周期变体，已补“但仅限今天”“仅限这周”“仅在学习 Python时”并保守拒绝。最终复核原审查问题剩余 0 项。

最终审查汇总：Standards 0 项未解决；Spec 0 项未解决。

## 真实百炼

命令：`python -m tests.live_memory`。使用后端本地凭据、临时隔离数据库和固定快照 `qwen3.7-plus-2026-05-26`，`enable_thinking=false`、`response_format=json_schema`、`strict=true`，HTTP 客户端关闭环境代理。结构化模式依据 [百炼官方文档](https://help.aliyun.com/zh/model-studio/qwen-structured-output)。

第一次提取成功，但“官方资料”未匹配新问题中的“资料”，迁移断言失败。修复匹配后迁移成功；随后“今天只有半小时”的候选主题被生成为原文没有的“时间”，应用拒绝保存。增加原文主题示例后真实输出为“半小时”，期限正确保存为本地次日零点。

实测中还出现把“这次只推荐视频”表述为“下次也推荐视频”的回复。已将本次例外与一般偏好隔离，并过滤未经提交的保存承诺、例外的永久化表述；真实检查脚本已增加相关断言。最终真实复跑的四个步骤全部通过：普通偏好保存、新会话迁移、当前例外不永久化、当天条件及次日零点期限。回复里的资料属于模型知识回答，本切片没有进行外部搜索或核实链接。

## 浏览器

使用真实 Chromium、真实本地应用和百炼，通过聊天输入普通偏好，界面显示 1 条已保存记忆、来源及生效信息，待办为 0。点击新会话提出 Python 生成器资料问题，显示此次加载 1 条、采用说明和“效果尚未验证”。截图 `output/playwright/issue-4-workbench.png`，快照保存在 `.playwright-cli/`（均为本地忽略文件）。第一次填充误用了标题元素，未产生提交，改用实际文本框后继续。

真实应用进程重启后刷新页面，原记忆与来源、上轮采用证据仍可查看。继续提交“今天只有半小时”，面板显示截止 `2026-09-21T00:00:00+08:00`。在浏览器专用隔离数据库安装插入失败触发器后提交“我喜欢游泳”，真实百炼仍返回普通回答，但页面终态为“部分完成”，明确显示“本轮学习保存失败”，记忆仍为原来的 2 条；截图 `output/playwright/issue-4-partial.png`。检查完已移除故障触发器。控制台 0 错误、0 警告。

## 限制

- 中国大陆出口位置未核验，因此大陆无代理验收仍为未验证；禁用环境代理不是出口地域证明。
- 首版采用保守中文直接陈述校验和词项匹配，可能漏掉复杂表达、跨句省略或同义主题；不支持的日期范围不当作永久记忆保存。
- 潜在冲突会暂停生效。明确纠正、面板编辑删除及删除防恢复在后续切片实现；本次没有提前实现这些功能。
- 采用说明不是效果验证；完整学习前后对比、搜索和天气不在本次范围。
