# Issue #1：首版总规格补验

日期：2026-09-21。本轮起点与固定审查基线：`8196b06c76c2513f6e18bb2438bf885141360175`。GitHub 读回 #2～#9 均 CLOSED，#1 OPEN。子任务关闭不代表总规格所有验收已通过。

**最新状态：真实百炼、IQS、和风及两条主线已完成本机联调；用户确认位于中国大陆并关闭代理/VPN后，完整真实组合再次通过。此前缺凭据与网络条件的验收阻塞已解除。以下保留首轮失败和修复历史，最终环境依据与限制见文末。**

## 本轮修复

1. 规格原始演示输入“我看技术资料喜欢先看官方文档，再做一个小练习。今天我要学 Python 生成器。”原先没有保存记忆或待办，转入搜索且因缺 IQS 返回 partial。现在完整保留顺序偏好，将独立日期意图保存为当天待办。学习候选只能使用原文完整分句；不丢弃后半步骤、相邻期限或原文否定授权。
2. 和风官方 API Host 示例现在包含多级子域，旧配置只接受一级子域而拒绝合法配置。现在保留并接受完整专属域名，继续拒绝外部后缀、用户信息、协议、路径及空标签，凭据不会因此被发送到任意域名。
3. 新增 [API 配置说明](../api-setup.md)，记录控制台入口、独立凭据、完整 Host 及真实验收命令。未改用户 `.env` 或日常数据库。

Host 格式依据于本日读取的 [和风官方文档](https://dev.qweather.com/docs/configuration/api-host/)。

## 红绿回归与真实调用

- 原始示例公开 HTTP/SSE 回归：初次 **1 failed、1 passed**，失败为未保存偏好。修复后学习/记忆维护/待办/证据聚焦 **83 passed**。
- 多级 Host 公开天气路径：初次 **1 failed、1 passed**，失败为配置验证拒绝官方示例。修复并加入错误目标检查后，天气与证据聚焦 **55 passed**。
- 中间 Ruff 检查发现新增长行和1个文件格式问题，均已修正；未掩盖为初次就通过。
- 真实百炼独立示例：`output/issue1/spec-before.json` 记录修复前 partial、0记忆、0待办；`spec-after.json` 记录 completed、1条完整偏好、1项当天待办。模型额外提出的1条无效候选被拒绝，不能表述为所有候选均通过。未调用真实搜索或天气。
- 组合入口 `python -m tests.live_acceptance --mock-information --output output/issue1/live` 已改为规格原始示例。`output/issue1/live/da5dc3fcaf/` **passed**：固定真实百炼模型、模拟 IQS/和风；实际启动3次进程、重启2次，核验新会话新主题、纠正/删除、仅建议不写入、显式接受防重复、完成读回、天气消歧/超范围/失败和检查点重启读回。模式、调用次数与耗时见 results.json，各轮证据与请求结构单独保存。
- 不带模拟参数的完整入口 `output/issue1/live/85015edd13/` **skipped**：本地缺 `IQS_API_KEY`、`QWEATHER_API_KEY`、`QWEATHER_API_HOST`；没有请求真实信息服务。不得按进程退出码0将此项计为通过。

## 浏览器

用独立 `output/issue1/browser.db`、真实百炼和真实后端，在浏览器输入规格原始示例。显示1项当天待办及1条完整偏好，刷新后仍存在；公开接口再次读回各1条。此场景不需要搜索或天气。

- 快照：`output/playwright/issue1-saved.yml`、`issue1-reloaded.yml`。
- 已查看截图：`issue1-reloaded.png`、`issue1-memory.png`，原文顺序偏好未截断，列表日期为2026-09-21。
- 数据读回：`output/issue1/browser-readback.json`；浏览器控制台0 errors、0 warnings。
- 未重新执行 #9 已记录的双标签、断网和窄屏流程；它们仍属于历史证据。

## Standards

独立只读审查 `8196b06…b622729`：文档规范违反0项，可操作启发式异味0项。角色、领域术语、公开 HTTP/SSE 与隔离 SQLite 的测试边界及真实/模拟区分符合既有规范。

## Spec

独立只读审查同一差异：可操作代码问题0项，未发现越界。审查者复验天气与证据 **55 passed**。来源完整性、范围期限及完整原文授权未被分句修复绕过。真实信息服务和大陆网络条件仍是未完成的规格要求。

## 首轮剩余交付条件（配置前历史）

沿用 [Issue #9 的 AC-01～AC-12 映射](issue-9.md)。本轮补齐了原始示例和合法 Host 的代码缺口，但 **AC-09/AC-10 真实 IQS、和风及 AC-12 大陆无代理联调尚未通过，首版完整交付未达成**。

后续需在本地配置独立凭据及 Host，确认实际大陆网络和代理/VPN状态，执行真实搜索、结果网站访问、城市与天气及两主线组合测试，记录实际结果后再判断 #1 是否满足关闭条件。本轮 implement 授权本地实现和当前 main 提交；未推送、创建 PR、合并或关闭 Issue。

## 首轮最终门禁

基于实现提交 `b622729`，审查后全量测试一次：**187 passed，2 warnings，61.24秒**。警告为既有 Starlette/httpx 和 anyio 弃用提示。mypy（14个应用源文件）、Ruff check、Ruff format（36文件）、前端 TypeScript/Vite build 和 `git diff --check` 均通过。之后仅更新文档，不改变应用与测试行为。

Standards 0项；Spec 0项可操作代码发现。真实服务缺少配置为 skipped，大陆网络为 unverified；两者不计为通过。

## 凭据配置后的真实联调

用户配置完成后，后端确认百炼、IQS、和风独立密钥和专属 Host 均非空；未输出密钥或账户域名。本阶段用户确认机器位于中国大陆但代理/VPN开启，所以以下结果仅代表当时网络，不能单独作为 AC-12。

### 首次失败和修正

- `output/issue1/live-weather-configured.log`：失败。真实城市查询 HTTP 200，对“上海”返回城市、下辖区及近似名称，应用正确要求消歧；旧脚本假设必定直接得到天气。脚本现从实际候选中核对 ID、名称和国家，再以完整地点 ID 和日期通过公开聊天继续；不改应用选址规则。
- `output/issue1/live/2886aeca6d/`：失败。装饰器资料的某个正文读取失败，应用保留摘要、来源、步骤和建议并标记 partial；旧脚本仅接受 completed。现只在研究状态也为 partial、存在 gaps 且仍有来源/步骤/建议、无擅自待办写入时接受，最终汇总保留 partial_steps。
- `output/issue1/live/39d538909e/`：失败。真实模型将记忆主题提取为“看官方文档”，普通自然纠正无法唯一匹配，应用要求明确 ID 且未修改。脚本现仅在确实收到该澄清时，使用刚保存的真实记忆 ID 通过聊天继续纠正，并验证 changed_ids 和后续新状态。不能将该失败记录描述为自然表述直接纠正成功。

这些修改只修复验收脚本的环境假设；应用业务代码未改。提交 `0620d55`、`e98dcde`、`f4336a8`。原失败目录与日志均保留，后续运行写新目录。

### 当前网络真实结果

- `live-research-configured.log`：通过真实百炼/IQS 流程。搜索和正文读取均有 HTTP 200；一项正文无效时保留摘要并报告 partial。结果网站独立访问分别观察到200和301；301仅证明重定向响应，不代表目标全文可读。返回资料多为第三方教程，不将查询中的“官方”当成官方来源命中证明。
- `live-weather-resolved.log`：通过真实城市与每日预报；上海日期2026-09-22，城市与天气请求均HTTP 200，各约0.6秒；未新增待办。
- `live/17be66a8fd/`：完整真实组合 **passed**。实际进程重启、跨会话迁移、纠正、删除、建议授权/防重复、完成状态和检查点重启读回通过。partial_steps 为 corrected（正文缺失）、outing（地点消歧）与 ambiguous（重名待澄清），没有包装成所有子步骤均完整成功。该次自然纠正直接匹配成功；不能替代上一条不同模型提取结果下的失败记录。

### 真实服务浏览器

独立 `output/issue1/browser-real.db`，真实百炼、IQS与和风。原始表达保存1条偏好和1项待办；新会话加载偏好，实际检索后仍为1项待办；明确点击“加入待办”后变为2项；天气地点消歧后查询真实预报，待办仍为2项。

快照见 `output/playwright/issue1-real-{learn,new-session,plan,accepted,city,weather}.yml`；已查看天气面板截图 `issue1-real-weather.png`。公开数据及执行记录见 `output/issue1/browser-real-readback.json`，浏览器控制台0 errors、0 warnings。此阶段代理/VPN尚开启。

### 审查与复验

Standards/Spec 独立审查 `338e6de…e98dcde`，以及增量 `e98dcde..f4336a8`，两轴均0项可操作发现。澄清后按实际 ID 重试、部分成功保留缺口符合规格，并非绕过验收。

第一次全量 **186 passed、1 failed**：`test_kill_after_learning_commit_keeps_receipt_and_retry_skips_learning` 在强制结束后的进程启动阶段退出，未取得子进程 stderr，原因尚未确定；不将其归因于已证实的应用错误或网络问题。关闭并发浏览器/联调进程后，单项复验 **1 passed**。后续完整复验单列，不覆盖初次失败。

同一应用实现下随后完整复验 **187 passed、2 warnings、63.46秒**。mypy、Ruff check、format及diff检查通过。前端代码未变化，沿用本轮首阶段已通过的TypeScript/Vite build。启动失败未再复现，不能据此声称已查明或修复其根因。

## 中国大陆直连补验

用户先确认机器位于中国大陆、当时代理/VPN开启，随后明确回复“已关闭，可以补跑直连验收”。此后才启动本节调用。环境确认记录位于 `output/issue1/mainland-live/network-context.json`；位置与关闭代理来自用户确认，没有独立IP归属探测，也没有修改用户网络设置。

完整命令：`python -m tests.live_acceptance --output output/issue1/mainland-live`。结果目录 `2af95862fa/`：**passed，全部使用真实百炼、IQS和和风**，包括学习保存、实际进程重启、新会话计划、新主题迁移、纠正、删除、建议接受防重复、完成读回、地点消歧、实际天气与检查点重启读回。记录固定模型、请求模式、用量、调用次数、耗时及来源，未重放完整旧会话。

脚本字段 `mainland_network: unverified` 表示脚本不能自动检测物理位置；不能单看该字段认定网络条件。本节验收依据是**用户明确确认的大陆直连环境 + 客户端 trust_env=False + 随后真实调用记录**，不冒充独立地理位置证明。

- `transfer` 和 `corrected` 为有来源与行动步骤的部分成功：某条正文未取得，仅保留其搜索摘要并说明缺口。
- `outing` 和 `ambiguous` 正确为待消歧；`outing-resolved` 使用实际候选上海 ID 查询对应日期的真实天气，城市/天气均HTTP 200。
- 结果网站另行验证，见同目录 `website-reachability.json`：廖雪峰生成器页面HTTP 200；CSDN移动链接经一次重定向后最终HTTP 200。只验证这两条样本，不推断所有结果网站都可达。
- AC-09/AC-10 的真实信息服务部分及 AC-12 的实际调用部分完成；网络条件按上述用户确认方式成立。两个主线的真实浏览器验证发生在关闭代理前，直连阶段用同一公开 HTTP/SSE 业务入口复验，不将前一阶段浏览器冒充直连浏览器。

## 最终结论与边界

本次解除真实服务凭据与大陆网络联调缺口，核心公开流程已真实跑通；187项本地回归最终通过，Standards/Spec均0项可操作代码发现。初次进程启动测试失败仍保留为原因未确定的一次性观察。

记忆影响了实际查询输入，纠正和删除后输入随之改变；真实搜索返回的资料多为第三方教程。**没有证明“偏好官方资料/视频”必定得到对应类型的来源**，不能把供应商可达、工具查询变化或模型采用说明等同于推荐质量已完全满足。自然语言匹配、搜索质量和首版既有保守限制继续适用。

所有本轮变更已本地提交，未推送、创建PR、合并或关闭Issue #1。缺凭据的旧状态仅作为历史保留；后续发布或质量改进应依据本节最新结果。
