# Issue #3 验收记录

日期：2026-09-20。需求：维护待办并生成基础当天计划。审查起点由用户确认：`7dc5f41199f81ecacefe2fe4b26858fe3725feb2`。测试边界由用户确认：真实浏览器、公开 HTTP/SSE、隔离真实 SQLite、受控时钟，以及配置就绪后的真实百炼调用。

## 实现与可观察行为

- 聊天、面板通过同一 messages/run/SSE 路径执行；面板显式操作无需模型。标题、日期、完成状态由应用校验并事务提交，稳定待办 ID 保持不变。
- 按接收时间的用户时区解析相对日期；查询用当前本地日期分组。今日、逾期、未安排、未来、已完成独立呈现，逾期不自动改期。
- 模型只能提出候选操作。聊天修改独立从原文解析唯一目标和字段，再对比模型候选；不明确、否定、多个目标或同名事项要求澄清。
- 已有待办部分从 SQLite 查询结果渲染，模型只生成独立的可选行动建议；建议保存来源执行、稳定 ID、计划日期和接受后的待办 ID。
- 生成计划不写待办；显式接受当前会话的一项建议才创建一次。跨会话引用建议被拒绝，重复接受和重复 request_id 不重复写入。
- 更新、新增、建议接受标记、成功事件及请求终态同事务提交。失败不产生成功保存事件；已有 #2 数据库启动时补充 action 列和建议表，保留旧数据。

## 确定性测试：通过

`python -m pytest -q`：35 passed，无跳过；有两条既有 Starlette/httpx/anyio 弃用警告。

新增维护场景覆盖稳定 ID、标题/日期/完成、请求重放与操作内容冲突、受控上海零点前后日期分组、相对日期、歧义目标、否定指令、建议授权、空清单、跨会话建议隔离、无模型面板校验。创建和查询使用 HTTP，所有状态断言从公开 API 读回真实 SQLite。

对修改、完成、建议加入分别注入 SQLite 写入失败：整个操作回滚，无 saved 事件；移除故障后原请求仍重放失败，新请求可成功。既有真实进程重启用例扩展到修改、完成、计划与建议：终止旧 Uvicorn，启动新进程，移除凭据，验证状态保留以及已接受建议不能重复创建。

TDD 记录：面板切片首次因不支持 action 返回 422；分组切片首次缺少 overview 接口；聊天修改切片首次无写入 ID；计划切片首次工具不受支持。实现各切片后分别转绿，最终全套通过。

静态检查：mypy、Ruff、Ruff format、TypeScript/Vite 生产构建、`git diff --check` 通过。

## 双轴审查

- Standards：未发现硬性规范违规或需要修改的代码异味。
- Spec：首轮发现一项 P2。两条建议中已接受一条后，“加进去”会按剩余未接受项自动选择，未充分处理歧义。公开接口回归测试先复现失败；修正为保留所有可能的建议目标，多项时必须明确标题或 ID，随后转绿。不存在因已接受状态而推定用户新意图的路径。
- 修复后 Spec 独立复核无遗留问题，并运行维护测试 9 passed；最终全套再次运行 35 passed。Standards 0 项，Spec 1 项已修复、0 项遗留。

## 真实百炼：通过

`python -m tests.live_maintenance` 使用本地后端配置、临时数据库及公开 HTTP/SSE。模型 `qwen3.7-plus-2026-05-26`，关闭深度思考，HTTP 客户端 `trust_env=False`。

- 首轮：创建、改日期、改标题、计划、建议加入成功；完成断言失败。原因是建议与原待办同名，加入后出现两个同名事项，按名称完成时未发生写入。没有把这轮记为通过。
- 改进计划提示，要求具体行动步骤；完成验收使用稳定 ID，避免依赖建议不与原待办重名。
- 第二轮：创建 → 改日期 → 改标题 → 当天计划 → 接受建议 → 按稳定 ID 完成 → 查询，七步全部通过，每步一次模型调用，总耗时约 14.805 秒。计划前后待办完全一致，最终两条待办，原事项已完成，建议对应新事项待完成。

## 实际浏览器：通过

Playwright CLI 驱动实际 Chromium，访问隔离数据库的 Uvicorn 服务；聊天和计划使用真实百炼，面板调用正常公开接口。

1. 聊天创建今天的 Python 学习事项和指定过去日期的整理书桌，页面正确分为今日、逾期。
2. 面板将整理书桌改名为整理书架并改到明天，列表从逾期移入未来安排，聊天显示真实新值。
3. 生成当天计划，仍为两条待办；页面单列三条具体行动建议。
4. 点击一项加入待办后总数变为三，按钮显示“已加入”且禁用；完成原学习事项后移入已完成。
5. 停止 Uvicorn 并用同一隔离数据库、空模型凭据重启；健康接口返回 `model_configured=false`。重新加载保留三条待办、修改结果、完成状态和建议接受状态。
6. 无模型时通过面板完成一项、修改另一项标题、清空日期转为未安排；开启新会话后仍显示真实保存状态。

本地截图：`output/playwright/issue-3-workbench.png`；浏览器快照位于 `.playwright-cli/`（均忽略，不提交）。浏览器控制台检查无错误或警告。清空日期的第一次 CLI run-code 调用语法失败，未执行该步骤；改用正确的函数形式后完成清空、保存并读回未安排分组。

## 范围与未验证项

- 网络出口地域未核验，因此中国大陆无代理验收仍为**未验证**，不因客户端关闭代理而宣称通过。
- 自然语言授权采取保守句式校验；复杂转述、多个目标或多个修改指令请拆开提交，或使用面板。不会根据模型猜测写入。
- 本切片未接入用户记忆、搜索、天气及外部任务执行，也未验证这些后续功能。
