# Issue #6：按需搜索资料并细化学习安排

日期：2026-09-20。测试边界经用户确认：公开 HTTP/SSE、真实临时 SQLite、浏览器操作；仅在外部供应商边界模拟。审查基线 `973e5cb0f2bc7b8c3cc5992d99de932b5b74999b`。

## 范围与设计

主 Agent 结合当前请求、真实待办、相关生效记忆提出 `research_learning`；执行 Agent 是只能搜索和读取资料的确定性执行器。搜索使用独立 IQS 凭据、固定官方 HTTPS 接口；不支持任意工具、外部写入或安装。主 Agent 的第二次调用只有返回步骤的工具，没有待办写入工具。引用 ID 必须对应实际资料；可选练习沿用现有建议、授权校验和防重复事务。

查询、输入摘要、实际请求、耗时、来源及四类结果通过 SSE 和 runs.research 持久保存。最多前 5 条资料、前 2 条正文，有限单次超时、整轮超时、共享调用和重试预算。正文读取失败保留摘要，摘要不标为正文。网页只进入最终组织上下文，不进入记忆提取。响应大小与展示长度有上限，禁止危险链接协议和内部 IP 地址。

## TDD / 本地接口

第一条搜索路径测试先因未实现失败后通过；空结果失败、鉴权故障导致 SSE 不结束（该测试进程已中止）、正文读取缺失、当天计划漏加载资料偏好均由后续切片修复。类型检查曾发现 HTTP 响应/模型返回变量重用，以及可空 summary 切片，已修复。

交接前研究测试 29 passed（包含用户新增的多任务选择要求）；本轮接续后为 33 passed，详见下方最终验证。首轮研究与既有记忆测试合跑 44 passed；此后针对中断终态、真实事件身份、显式搜索与咨询边界补充回归。覆盖搜索/不搜索、原始来源字段、独立凭据、默认费用参数、四种结果、正文成功和失败、单次及整轮时限、调用预算、越权模型工具、假引用、恶意 URL、建议加入一次，以及无凭据重建应用后的持久记录和重放。自动化重启项重建应用，不冒充真实 OS 进程重启。浏览器验证中实际终止、重启 Uvicorn 后，同一隔离数据库中的旧消息、已接受练习和资料仍可读。

## 浏览器与真实调用

Chromium + 真实本地 HTTP/SSE/SQLite + 模拟外部供应商：来源链接、仅摘要标签、实际工具调用展示、生成后待办为 0、点击加入后为 1、查询待办不搜索。追加正文成功/失败浏览器验证：成功显示“已取得正文”并可展开实际文本、2 次调用；供应商返回站点 4030 时仅保留摘要并明确缺少正文，终态部分完成。截图 `output/playwright/issue-6-sources.png`、`issue-6-body.png` 已实际查看；另有 `issue-6-body-failure.png` 与 `issue-6-real-missing-key.png`。最终控制台 0 错误和 0 警告。

真实百炼（无模拟模型）：第一次浏览器请求“查找 Python 生成器的官方学习资料，并给我一个练习”误选创建待办，应用保守授权校验拒绝，待办仍为 0。另一次公开 HTTP/SSE 请求正确选择 research_learning、调用数 1；因 IQS 凭据缺失终态 partial，来源为空，明确提示未配置。随后一次请求又误选 list_todos，收紧提示词后仍有误选 create_todos。最终对明确搜索指令约束唯一研究工具（查询参数仍由模型生成），并校验返回工具身份；保留费用/方法咨询、否定、引用与写入请求的普通模型选择。最终真实浏览器复测同句正确生成查询、研究状态 error、整轮 partial、工具调用 0，明确显示缺独立凭据。首次和中间失败均不记为通过。

`python -m tests.live_research` 已实际运行，结果 **SKIP**：本地未配置独立 `IQS_API_KEY`。脚本会使用隔离数据库验证真实搜索、正文、跨会话新主题偏好迁移、建议接受与不搜索，并分别记录 IQS 请求和结果网站的本机访问状态与耗时。

## 真实模型与模拟 IQS 的组合验证

额外通过公开 HTTP/SSE、真实百炼、模拟 IQS、临时 SQLite 检查真实模型与工具协议协作，明确不是 IQS 真调用。先保存官方资料偏好，再创建当天 Python 生成器待办，在新的会话查询当天计划及资料，之后再开新会话查 JavaScript Promise。首次组合验证里当天计划仅建议搜索、没有真正委派；修正 plan_day 提示冲突与显式搜索分句识别后，最终两次查询均 completed / research success，均加载并采用 1 条偏好。

最终查询分别为“Python 生成器官方文档教程和练习题”和“JavaScript Promise 学习资料和练习，优先官方文档”；资料字段来自模拟供应商，步骤与练习由真实百炼产生。日志 `output/issue6-hybrid.jsonl` 保留初次失败，`output/issue6-hybrid-final.jsonl` 保留最终观察结果。这些结果不能代替 IQS 账户及大陆可用性验收。

## Standards

初审 1 项 P2：HTTP 200 尚未校验必要字段就发成功事件，随后又发错误。已改为完整协议校验后发布单一结果；公开 SSE 回归先失败后通过。独立复核 0 项未解决问题。

## Spec

初审 2 项 P2：中断后研究记录仍 running；研究建议虚记 plan_day 成功。已在异常终态事务中同步研究与调用状态，并使用真实工具身份；公开回归先失败后通过。增量审查发现 1 项 P2：搜索费用/方法咨询被强制成外部查询，先按关键词排除仍漏掉其他咨询；最终保留分句问号，匹配分句为问句时使用普通模型选择，新增负例通过。该问题最终复核已修复。用户随后要求多个任务供选择，新增默认 3–5 项、明确数量与逐项接受；交接时 Standards 0 项未解决，Spec 留有 1 项 P2：数量解析未识别“两个可选学习任务/两个学习任务”，返回正确两项会被按默认数量拒绝。本轮接续先复现这两个失败，再增加必要修饰语匹配后通过。最终基于原固定点及工作区差异独立双轴复核，Standards 0 项、Spec 0 项未解决代码问题；真实 IQS 与大陆网络验收仍为未完成的外部条件。

## 未完成的真实验收

- 真实 IQS 搜索接口、正文读取和实际结果网站可达尚未验证。
- 中国大陆出口位置未核实；客户端 trust_env=False 只能说明禁用环境代理。
- 模型路由存在非确定性；本地模拟测试不能证明真实模型始终正确选路。
- 本次不关闭 Issue #6，不将真实服务验收标为通过。代码提交仅在当前本地分支。

## 官方协议依据

- [UnifiedSearch 请求/响应字段](https://help.aliyun.com/zh/document_detail/2963346.html)：独立凭据、pageItems、snippet 和增强摘要选项。
- [IQS HTTP 接入](https://help.aliyun.com/zh/document_detail/3029866.html)：搜索与正文读取地址和认证。
- [ReadPageBasic](https://help.aliyun.com/en/document_detail/2983380.html)：data.statusCode/text、页面时限及返回正文。

首轮全量 107 passed；后续路由修复后全量 111 passed（33.92 秒）。该次检查期间新增回归文件尚未格式化，Ruff/format 当时失败；随后完成格式化并对最终版本重新检查，不能将这次检查记为全绿。随后一次全量 114 passed（32.90 秒），mypy、Ruff、format、diff-check 通过；这次在最新多任务变更之前。最新多任务版本研究文件 29 passed（6.00 秒），frontend TypeScript/Vite build 通过；最近一次 mypy 和 Ruff 在多任务核心代码加入后通过。上述为交接前历史结果；本轮最新代码的完整检查见下方，旧结果不替代最终验证。

## 用户新增要求与交接时状态

用户希望 Agent 生成多个任务供选择。已实现默认 3–5 个可选学习任务、明确指定数量时覆盖默认、逐项选择加入；公开三选二测试证明只写入选择的两项，未选项保持建议。最新真实百炼 + 模拟 IQS 组合验证分别为 Python 计划和 JavaScript Promise 生成了 3 个不同候选，均 completed / research success；日志 output/issue6-multiple.jsonl。该日志中的断言验证了两个主题的候选数范围。交接时多候选 UI 的逐项选择尚未额外浏览器复测；本轮接续已完成，见下方。

交接时用户要求在新对话继续修复数量问题，应用代码停在 7f3c34b，三份说明文档未提交。本轮保留这些文档内容并按最终结果更新，与修复一起本地提交；不推送、不修改或关闭 Issue。


## 2026-09-20 接续后的最终验证

- **数量修复 TDD**：公开请求的三个措辞变体先得到 2 failed / 1 passed（两个带修饰语请求返回 partial）；仅扩展“可选／学习”修饰语后研究文件 31 passed。随后扩展候选数量与防重复验证，最终研究文件 33 passed（6.91 秒）。
- **默认候选与授权**：通过公开 HTTP/SSE 和真实临时 SQLite 分别验证供应商返回 3、4、5 个候选，生成时待办为 0；逐项选第 1、3 项后仅保存对应两项，其余项的 todo_id 为空。同请求重放保留原执行与 SSE；新请求再次接受已选项提示已加入，没有新增。扩展测试首次有 3 项失败，是误把新请求重复接受的预期写成返回原 todo_ids；已按既有“本轮不新增”的接口契约修正，未修改生产接受事务。
- **浏览器**：Playwright CLI 驱动真实 Chromium，Uvicorn HTTP/SSE 和隔离 SQLite，模型与 IQS 均模拟。默认生成 3 个不同候选；点击第 1、3 项后“已保存的待办 2”，其按钮禁用并显示“已加入”，第 2 项仍有“加入待办”。刷新页面后这些状态保留。新会话输入“搜索 Python 生成器资料，给我两个可选学习任务”正常完成并显示两项，本轮保存 0 项待办。控制台 0 错误、0 警告。截图 `output/playwright/issue-6-select-two.png` 已实际查看；对应浏览器会话 `issue6-continue`。本轮验证的是刷新和新会话，未新增 OS 重启验证。
- **完整检查**：`python -m pytest -q`：123 passed（36.16 秒），2 条现有 FastAPI/Starlette 依赖弃用警告。`python -m mypy`：12 个源文件通过；Ruff check、format check（25 文件）、前端 TypeScript/Vite build、`git diff --check` 均通过。
- **审查**：两个独立子代理按固定基线 `973e5cb0f2bc7b8c3cc5992d99de932b5b74999b` 审查完整实现及本轮工作区修改。Standards：0 项；Spec：0 项未解决代码问题。最终文档同步后再次核对证据与限制。
- **真实服务**：本轮重新运行 `python -m tests.live_research`，仍为 **SKIP real IQS: independent IQS_API_KEY is not configured**。没有将模拟供应商或此前真实百炼加模拟 IQS 的结果算作真实 IQS 通过；真实搜索、正文、结果网站可达与大陆出口位置仍未验证。本轮未重复真实百炼组合检查。
